# Help screenshots

Drop the GitHub fine-grained token walkthrough screenshots here so they appear
in the in-app Help & FAQ (the FAQ references these exact filenames and hides any
that are missing):

- token-1.png — Settings → Developer settings
- token-2.png — Personal access tokens → Fine-grained tokens → Generate new token
- token-3.png — Token name / Resource owner (your account) / Expiration / All repositories
- token-4.png — Permissions: Actions + Contents + Administration = Read and write (Metadata read-only)
- token-5.png — Generate token confirmation dialog
- token-6.png — Token created — copy it now (shown only once)
